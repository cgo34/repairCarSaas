
export interface IClient {
  // get(): any; // Retourne un client générique, sans méthodes spécifiques
  // auth: any; // Expose auth pour éviter les erreurs
  
  // rpc<T = any>(fn: string, params?: Record<string, any>): Promise<{ data: T | null; error: any }>;
}

